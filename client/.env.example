# Copy this file to .env and adjust for your environment. Never commit .env itself.

# Backend API base URL (must include /api/v1)
VITE_API_URL=http://localhost:5000/api/v1

# Frontend's own origin — used to build personalization deep links
# (utils/personalizationLink.ts)
VITE_FRONTEND_URL=http://localhost:5173
